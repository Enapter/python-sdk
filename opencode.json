{
    "$schema": "https://opencode.ai/config.json",
    "permission": {
        "bash": {
            "pipenv run pytest *": "allow",
            "pipenv run black *": "allow",
            "pipenv run isort *": "allow",
            "pipenv run pyflakes *": "allow",
            "pipenv run mypy *": "allow"
        }
    }
}
